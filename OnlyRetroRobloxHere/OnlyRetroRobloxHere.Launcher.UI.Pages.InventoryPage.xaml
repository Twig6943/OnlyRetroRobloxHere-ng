<?xml version="1.0" encoding="utf-8"?>
<local:BasePage x:Class="OnlyRetroRobloxHere.Launcher.UI.Pages.InventoryPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:OnlyRetroRobloxHere.Launcher.UI.Controls" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:OnlyRetroRobloxHere.Launcher.UI.Pages" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:viewmodels="clr-namespace:OnlyRetroRobloxHere.Launcher.UI.ViewModels.Pages" Title="InventoryPage" FontFamily="Verdana" TextOptions.TextFormattingMode="Display">
  <Grid Margin="10">
    <controls:StandardBoxCustomHeader>
      <controls:StandardBoxCustomHeader.HeaderContent>
        <Grid>
                    <Button HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="12" Foreground="{DynamicResource Theme.Accent.Highlight}" Content="{Binding Path=HammerTimeText, Mode=OneWay}" Click="OnHammerTimeClicked" />
                    <Button Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Center" Content="[ wipe ]" FontSize="12" Foreground="{DynamicResource Theme.Accent.Highlight}" Click="OnWipeClicked" />
          <TextBlock HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="16" FontWeight="Bold" Foreground="#FFFFFFFF" Text="Inventory" />
        </Grid>
      </controls:StandardBoxCustomHeader.HeaderContent>
      <Grid Cursor="{Binding Path=RegularCursor, Mode=OneWay}">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <controls:PaginationItemsControl x:Name="InventoryControl" Grid.Row="2" Columns="6" PaginationTextFormat="Page [[[0]]] of [[[1]]]" Rows="6" MainItemsSource="{Binding Path=Inventory, Mode=OneTime}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Button MinWidth="75" MaxWidth="150" Margin="0,0,2,0" Padding="0" VerticalAlignment="Top" HorizontalContentAlignment="Center" Background="#00FFFFFF" BorderThickness="0" Style="{StaticResource ButtonNoEffects}" Cursor="{Binding Path=DataContext.HoverCursor, Mode=OneWay, RelativeSource={RelativeSource AncestorType={x:Type Page}}}" Tag="{Binding Path=Id, Mode=OneWay}" Click="OnDeleteClicked">
                <Grid>
                  <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                  </Grid.RowDefinitions>
                  <Border Width="75" Height="75" Background="{StaticResource ItemImageBackground}">
                    <controls:FancyLazyImage Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Top" RenderOptions.BitmapScalingMode="HighQuality" ImagePath="{Binding Path=Image, Mode=OneWay}" />
                  </Border>
                  <Viewbox Grid.Row="1" Stretch="Uniform" StretchDirection="DownOnly">
                    <TextBlock HorizontalAlignment="Center" Style="{StaticResource TextBlockWhite}" TextWrapping="Wrap" Text="{Binding Path=Name, Mode=OneWay}" />
                  </Viewbox>
                </Grid>
              </Button>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </controls:PaginationItemsControl>
        <Grid Row="3" HorizontalAlignment="Center" Visibility="{Binding ElementName=InventoryControl, Path=AnyPages, Converter={StaticResource BoolToVisibilityConverter}}">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
                    <Button Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Background="#00FFFFFF" BorderThickness="0" FontSize="12" Foreground="{DynamicResource Theme.Accent.Highlight}" Style="{StaticResource ButtonNoEffects}" Command="{Binding ElementName=InventoryControl, Path=PreviousPageCommand}">
            <StackPanel Orientation="Horizontal">
              <TextBlock VerticalAlignment="Center" FontSize="8" FontWeight="Bold" Text="&lt;" />
              <TextBlock Margin="-2,0,0,0" VerticalAlignment="Center" FontSize="8" FontWeight="Bold" Text="&lt;" />
              <TextBlock Text=" Back" />
            </StackPanel>
          </Button>
          <TextBlock Grid.Row="3" Grid.Column="1" Margin="5,0" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Style="{StaticResource TextBlockWhite}" Text="{Binding ElementName=InventoryControl, Path=PaginationText}" />
                    <Button Grid.Row="4" Grid.Column="2" VerticalAlignment="Center" Background="#00FFFFFF" BorderThickness="0" FontSize="12" Foreground="{DynamicResource Theme.Accent.Highlight}" Style="{StaticResource ButtonNoEffects}" Command="{Binding ElementName=InventoryControl, Path=NextPageCommand}">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="Next " />
              <TextBlock VerticalAlignment="Center" FontSize="8" FontWeight="Bold" Text="&gt;" />
              <TextBlock Margin="-2,0,0,0" VerticalAlignment="Center" FontSize="8" FontWeight="Bold" Text="&gt;" />
            </StackPanel>
          </Button>
        </Grid>
      </Grid>
    </controls:StandardBoxCustomHeader>
  </Grid>
</local:BasePage>